import React from 'react'

import './Styles.css';

const PrimaryButton = (props) => {
  return (
    <>

    <button id='Primarybtn'><p>{props.btnText}</p></button>
    </>
  )
}

export default PrimaryButton

